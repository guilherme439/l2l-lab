num_games: 1

agents:
  p1:
    agent_type: "policy"
    policy:
      name: "example"
      checkpoint: 100

  p2:
    agent_type: "random"


      
env:
  name: "scs"
  kwargs:
    config: "/path/to/game_config.yml"
    action_mask_location: "obs"
    obs_space_format: "channels_first"
